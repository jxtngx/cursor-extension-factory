# Walking skeleton — commands-ui

After spec approval: `package.json` contributes.commands + optional webview view.
`src/extension.ts` registers one command. Tests invoke it.
Do not copy this into src/ during `@init-extension`.
